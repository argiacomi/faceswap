[tool.flake8]
max-line-length = 99
max-complexity=10
statistics = true
count = true
exclude = [".git", "__pycache__"]
per-file-ignores = ["__init__.py:F401"]

[tool.pylint.DESIGN]
min-public-methods = 1
max-args = 10
max-attributes = 10
max-positional-arguments = 10

[tool.pylint.TYPECHECK]
generated-members = ["av", "cv2"]

[tool.ruff]
line-length = 99
target-version = "py310"

extend-exclude = [
    "lib/gui/control_helper.py",
    "lib/gui/popup_configure.py",
    "lib/logger.py",
    "lib/utils.py",
    "tests/lib/model/nn_blocks_test.py",
    "tests/lib/training/data_augmentation_test.py",
    "tests/lib/training/lr_finder_test.py",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"**/__init__.py" = ["F401"]
"tests/**/*.py" = ["E402"]
"tools/**/*.py" = ["E402"]

[tool.mypy]
python_version = "3.10"
files = ["lib/gui/services", "lib/gui/models"]
mypy_path = "."
follow_imports = "skip"
show_error_codes = true
warn_redundant_casts = true
warn_return_any = true
warn_unused_ignores = true
no_implicit_optional = true

[[tool.mypy.overrides]]
module = [
    "fastcluster.*",
    "ffmpeg.*",
    "ffmpy.*",
    "h5py.*",
    "keras.*",
    "lightgbm.*",
    "numexpr.*",
    "pexpect.*",
    "pynvml.*",
    "scipy.*",
    "sklearn.*",
    "tensorboard.*",
    "torch.*",
    "torchvision.*",
    "tqdm.*",
    "win32console.*",
    "winpty.*",]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = ["ignore::DeprecationWarning:keras.*:"]

[tool.pyright]
reportUnsupportedDunderAll = false
