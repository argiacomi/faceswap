#!/usr/bin/env python3
"""Landmark ensemble tool helpers.

This package intentionally has no ``cli.py`` yet. ``tools.py`` auto-registers any
tool CLI it finds, so the first slice remains import-only until plugin and CLI
integration are designed.
"""
