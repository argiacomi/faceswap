# threshold is in robust z-score units computed per model/landmark from the
# distance to the ensemble median.
method: hard_drop
threshold: 3.5
available_methods:
  - none
  - hard_drop
  - downweight
  - weighted_median
